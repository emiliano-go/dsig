{
    "name": "dsig",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "description": "GPG message signing for Vencord / Vesktop",
    "license": "GPL-3.0-or-later",
    "scripts": {
        "test": "node --import ./tests/register.mjs --test \"tests/**/*.test.ts\""
    }
}
